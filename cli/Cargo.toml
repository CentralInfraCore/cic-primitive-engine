[package]
name = "cic-primitive-engine-cli"
description = "Command line front end for the CIC primitive engine."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "cic-primitive"
path = "src/main.rs"

[dependencies]
cic-primitive-engine = { path = "../engine" }
